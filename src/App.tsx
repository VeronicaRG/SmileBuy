import React from 'react';

import { View } from 'react-native';

import Theme from './theme';

const App: React.FC = () => {
  return (
    <Theme>
      <View />
    </Theme>
  );
};

export default App;
